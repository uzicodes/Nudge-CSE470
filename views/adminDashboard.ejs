<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin Dashboard</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Bootstrap Icons -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
  <style>
    :root {
      --primary-blue: #0d6efd;
      --secondary-blue: #86b7fe;
    }

    body {
      background-color: #f8f9fa;
    }

    .dashboard-card {
      transition: transform 0.2s;
      border: none;
      border-radius: 15px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }

    .dashboard-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 6px 12px rgba(0, 0, 0, 0.15);
    }

    .card-icon {
      font-size: 2.5rem;
      color: var(--primary-blue);
    }
  </style>
</head>

<body>
  <%- include('partials/navbar') %>

    <div class="container-fluid mt-4">
      <div class="row">
        <div class="col-md-10 mx-auto">
          <h2 class="mb-4"><i class="bi bi-speedometer2 me-2"></i>Admin Dashboard</h2>

          <div class="row g-4">
            <!-- User Requests Card -->
            <div class="col-md-6">
              <div class="card dashboard-card h-100">
                <div class="card-body">
                  <div class="d-flex align-items-center mb-3">
                    <i class="bi bi-people-fill card-icon me-3"></i>
                    <h5 class="card-title mb-0">User Requests</h5>
                  </div>
                  <p class="card-text text-muted">Manage and review user registration requests and permissions.</p>
                  <a href="/admin/requests" class="btn btn-primary">
                    Manage Requests <i class="bi bi-arrow-right-short"></i>
                  </a>
                </div>
              </div>
            </div>

            <!-- Feedback Card -->
            <div class="col-md-6">
              <div class="card dashboard-card h-100">
                <div class="card-body">
                  <div class="d-flex align-items-center mb-3">
                    <i class="bi bi-chat-left-text-fill card-icon me-3"></i>
                    <h5 class="card-title mb-0">User Feedback</h5>
                  </div>
                  <p class="card-text text-muted">Review and analyze user feedback and suggestions.</p>
                  <a href="/admin/feedback" class="btn btn-primary">
                    View Feedback <i class="bi bi-arrow-right-short"></i>
                  </a>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
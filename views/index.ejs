<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Fitness Hub - Home</title>
  <!-- Bootstrap 5 CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Bootstrap Icons -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.1/font/bootstrap-icons.css">
  <style>
    :root {
      --primary-blue: #0066cc;
      --secondary-blue: #0099ff;
      --dark-blue: #004080;
      --light-blue: #e6f2ff;
    }

    body {
      background-color: #ffffff;
      color: var(--dark-blue);
    }

    .hero-section {
      position: relative;
      height: 70vh;
      min-height: 500px;
      background: linear-gradient(rgba(0, 102, 204, 0.8), rgba(0, 153, 255, 0.8));
      display: flex;
      align-items: center;
      justify-content: center;
      text-align: center;
      color: white;
      overflow: hidden;
    }

    /* Add a pseudo-element for the background image */
    .hero-section::before {
      content: "";
      position: absolute;
      top: 0; left: 0; right: 0; bottom: 0;
      background-image: url('https://i.ibb.co/TMSn4z70/fitness-center.jpg');
      background-size: cover;
      background-position: center;
      filter: blur(8px); /* Blur effect */
      opacity: 0.6;      /* 60% opacity */
      z-index: 0;
    }

    /* Make sure content is above the blurred image */
    .hero-section > .container {
      position: relative;
      z-index: 1;
    }

    .btn-primary {
      background-color: var(--primary-blue);
      border-color: var(--primary-blue);
      padding: 12px 30px;
      font-size: 1.1rem;
    }

    .btn-primary:hover {
      background-color: var(--secondary-blue);
      border-color: var(--secondary-blue);
    }

    .card {
      border: none;
      border-radius: 15px;
      transition: transform 0.3s;
      background-color: var(--light-blue);
    }

    .card:hover {
      transform: translateY(-10px);
      box-shadow: 0 10px 20px rgba(0, 102, 204, 0.1);
    }

    .feature-icon {
      font-size: 2.5rem;
      color: var(--primary-blue);
      margin-bottom: 1rem;
    }

    footer {
      background-color: var(--dark-blue);
      color: white;
      margin-top: auto;
    }

    .social-link {
      color: white;
      transition: color 0.3s;
    }

    .social-link:hover {
      color: var(--secondary-blue);
    }

    @media (max-width: 768px) {
      .hero-section {
        height: 50vh;
      }

      .hero-content h1 {
        font-size: 2rem;
      }
    }
  </style>
</head>

<body class="d-flex flex-column min-vh-100">

  <!-- Hero Section -->
  <section class="hero-section">
    <div class="container">
      <h1 class="display-4 fw-bold mb-4">Transform Your Fitness Journey </h1>
      <p class="lead mb-5">Join our community & achieve your health goals</p>
      <a href="/register" class="btn btn-primary btn-lg">Get Started with Nudge</a>
    </div>
  </section>

  <!-- Features Section -->
  <main class="container my-5 flex-grow-1">
    <div class="row g-4 text-center">
      <div class="col-md-4">
        <div class="card py-4">
          <div class="card-body">
            <i class="bi bi-person-circle feature-icon"></i>
            <h3>Member Login</h3>
            <p>Access your personalized dashboard</p>
            <a href="/login" class="btn btn-outline-primary">Sign In</a>
          </div>
        </div>
      </div>
      <div class="col-md-4">
        <div class="card py-4">
          <div class="card-body">
            <i class="bi bi-people-fill feature-icon"></i>
            <h3>New Member</h3>
            <p>Start your fitness journey today</p>
            <a href="/register" class="btn btn-primary">Join Now</a>
          </div>
        </div>
      </div>
      <div class="col-md-4">
        <div class="card py-4">
          <div class="card-body">
            <i class="bi bi-shield-lock feature-icon"></i>
            <h3>Admin Portal</h3>
            <p>Management dashboard</p>
            <a href="/admin/login" class="btn btn-secondary">Admin Login</a>
          </div>
        </div>
      </div>
    </div>
  </main>

  <!-- Footer -->
  <footer class="py-4">
    <div class="container">
      <div class="row">
        <div class="col-md-6 text-center text-md-start mb-3 mb-md-0">
          <p class="mb-0">&copy; 2025 Nudge. All rights reserved.</p>
        </div>
        <div class="col-md-6 text-center text-md-end">
          <a href="#" class="social-link me-3"><i class="bi bi-facebook"></i></a>
          <a href="#" class="social-link me-3"><i class="bi bi-instagram"></i></a>
          <a href="#" class="social-link me-3"><i class="bi bi-twitter"></i></a>
          <a href="#" class="social-link"><i class="bi bi-linkedin"></i></a>
        </div>
      </div>
    </div>
  </footer>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
